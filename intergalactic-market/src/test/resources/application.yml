spring:
  application:
    name: cosmocats-market

application:
  product-service:
    base-path: http://localhost:8089
    products: ${application.product-service.base-path}/api/v1/products

tests:
  unit-tests:
    enabled: true